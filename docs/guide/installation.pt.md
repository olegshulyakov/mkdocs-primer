# Instalação

Instale o tema a partir do PyPI:

```console
$ pip install mkdocs-primer
```

Depois selecione-o no `mkdocs.yml`:

```yaml
theme:
  name: primer
```

É toda a configuração — cada opção abaixo tem um valor padrão que funciona.
