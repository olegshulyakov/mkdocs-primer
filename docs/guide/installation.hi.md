# इंस्टॉलेशन

थीम को PyPI से इंस्टॉल करें:

```console
$ pip install mkdocs-primer
```

फिर उसे `mkdocs.yml` में चुनें:

```yaml
theme:
  name: primer
```

बस इतना ही सेटअप है — नीचे दिए हर विकल्प का एक कारगर डिफ़ॉल्ट मान है।
