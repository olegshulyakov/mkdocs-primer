# Installation

Installez le thème depuis PyPI :

```console
$ pip install mkdocs-primer
```

Puis sélectionnez-le dans `mkdocs.yml` :

```yaml
theme:
  name: primer
```

C'est toute la configuration : chaque option ci-dessous a une valeur par défaut qui fonctionne.
