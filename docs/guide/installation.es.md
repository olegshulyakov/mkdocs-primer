# Instalación

Instala el tema desde PyPI:

```console
$ pip install mkdocs-primer
```

Después selecciónalo en `mkdocs.yml`:

```yaml
theme:
  name: primer
```

Esa es toda la configuración: cada opción de abajo tiene un valor por defecto que funciona.
