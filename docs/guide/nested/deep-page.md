# A deeply nested page

This page exists so the sidebar has more than one level of nesting to render,
and so the breadcrumb trail has a page with two ancestors to draw.

The elements it used to demonstrate are on [Elements](../../elements.md), where
they are easier to find and where every admonition type is drawn rather than
described.
