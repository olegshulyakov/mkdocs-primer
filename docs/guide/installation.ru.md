# Установка

Установите тему из PyPI:

```console
$ pip install mkdocs-primer
```

Затем выберите её в `mkdocs.yml`:

```yaml
theme:
  name: primer
```

Это вся настройка — у каждой опции ниже есть рабочее значение по умолчанию.
