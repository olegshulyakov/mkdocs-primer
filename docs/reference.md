# Reference

The theme has no Python API to call — the package below exists only to carry the
templates and the `mkdocs.themes` entry point. It is rendered here by
[mkdocstrings](guide/plugins.md#mkdocstrings) so that the theme's handling of
that plugin's markup is exercised on every build.

::: mkdocs_primer
